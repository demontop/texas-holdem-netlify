{
  "name": "texas-holdem-netlify",
  "version": "1.0.0",
  "private": true,
  "description": "Netlify-deployable multiplayer Texas Hold'em web app with auth, admin chip recharge, and realistic poker table UI.",
  "type": "commonjs",
  "scripts": {
    "dev": "node scripts/dev-server.mjs",
    "build": "node scripts/build-check.mjs"
  },
  "dependencies": {
    "@netlify/blobs": "latest"
  },
  "engines": {
    "node": ">=20"
  }
}
